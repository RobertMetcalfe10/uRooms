<shape xmlns:android = "http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <gradient
        android:angle="270"
        android:endColor="@color/login_gradEnd"
        android:centerColor="@color/login_gradMiddle"
        android:startColor="@color/login_gradStart"
        android:type="linear" />
</shape>